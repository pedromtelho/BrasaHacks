import 'package:flutter/material.dart';

class CustomCard {
  List data;
  Color colorCard;
  String textCard;
  Icon icon;
  String onTap;
  CustomCard({this.colorCard, this.textCard, this.icon, this.onTap, this.data});
}
